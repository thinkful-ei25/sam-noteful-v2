language: node_js
node_js: node

services:
  -progresql

env:
    TEST_DATABASE_URL: pes://postgres@localhost/noteful-test
ostgr
before_script:
    - psql -c 'CREATE DATABASE "noteful-test";' -U postgres